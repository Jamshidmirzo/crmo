import 'package:flutter/material.dart';

class MyGroupdoshlarScreen extends StatelessWidget {
  const MyGroupdoshlarScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}